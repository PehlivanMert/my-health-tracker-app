[build]
  command = "npm run build"  
  functions = "netlify/functions"
  publish = "dist"  # build yerine dist kullanın

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200